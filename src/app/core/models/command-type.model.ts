export enum CommandType {
  CD = 'cd',
  DEFAULT = 'default',
  DISPLAY = 'display',
  HELP = 'help',
  LS = 'ls',
  LANG = 'lang',
  NOT_FOUND = 'notFound',
  MSG = 'msg',
  WGET = 'wget',
  MSG_INPUT = 'msgInput',
}
